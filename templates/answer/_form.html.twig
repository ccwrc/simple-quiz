<div class="row">
    <div class="col-12">
        <div class="card-box">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-outline-primary">{{ button_label|default('Save') }}</button>
            <br/><br/>
            {{ form_end(form) }}
        </div>
    </div>
</div>