{% extends 'base.html.twig' %}

{% block title %}Solve Quiz{% endblock %}

{% block body %}
    <h1>Solve Quiz</h1>

    <div class="row">
        <div class="col-12">
            <div class="card-box">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-outline-primary">{{ button_label|default('Confirm') }}</button>
                <br/><br/>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}