{% extends 'base.html.twig' %}

{% block title %}Main page{% endblock %}

{% block body %}
    <h3>options</h3>

    <p>
        <a href="{{ path('quiz_index') }}">quiz (crud)</a>
    </p>

    <p>
        <a href="{{ path('question_index') }}">question (crud)</a>
    </p>

    <p>
        <a href="{{ path('answer_index') }}">answer (crud)</a>
    </p>

{% endblock %}